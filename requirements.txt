# ---- Core ----
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
psycopg[binary]==3.1.19            # PostgreSQL driver (psycopg3)
django-environ==0.11.2             # 12-factor env config
drf-spectacular==0.27.2            # OpenAPI schema
django-cors-headers==4.3.1

# ---- Realtime / async (used from Phase 3, wired now) ----
channels==4.1.0
channels-redis==4.2.0
celery==5.4.0
redis==5.0.4

# ---- Auth / security ----
argon2-cffi==23.1.0                # strong password hashing

# ---- Geo / AI storage ----
# GeoDjango (django.contrib.gis) ships with Django; needs GDAL/GEOS system libs.
pgvector==0.2.5                    # vector column for AI chatbot KB (Phase 3)

# ---- Storage / files ----
django-storages[boto3]==1.14.3     # S3-compatible object storage

# ---- Server ----
whitenoise==6.6.0                  # static files in production (no nginx needed)
gunicorn==22.0.0
uvicorn[standard]==0.29.0          # ASGI worker for Channels

# ---- Dev / test ----
pytest==8.2.0
pytest-django==4.8.0
factory-boy==3.3.0
